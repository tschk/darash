{
  "name": "darash-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker serving the darash static site and API with live request analytics",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4",
    "linkedom": "^0.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "wrangler": "^4.13.0"
  }
}
